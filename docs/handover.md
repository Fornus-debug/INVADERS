# Handover

## 次回Codexへの指示例

`katakata-invaders.html` を正本として、Fornus Portfolio のページ切り替え型構成を確認してください。特に `initPortfolioApp()` 内の hash route、Canvasメニュー、KATAKATA BOT、`LINKS` 管理を見てください。LINEは `https://lin.ee/RnwJwMO`、Eメールは `mailto:fornus.fujiwara@gmail.com` で有効化済みです。ココナラ / ランサーズ / X の実URLが決まった場合は `LINKS` を更新し、スマホ幅 375px / 390px / 430px / 768px / 1024px で横スクロールとボタン操作を確認してください。

## 注意

- `src/` が作成された場合は、どちらを正本にするか再確認する。
- 旧LPセクションは削除せずCSSで非表示になっている。
- 外部AI API、`.env`、APIキーは不要。
